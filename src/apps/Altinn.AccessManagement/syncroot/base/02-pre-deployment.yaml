apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: pre-deployment
spec:
  interval: 10m
  path: ./fake
  prune: true
  wait: true
  force: true
  targetNamespace: product-accessmanagement

  sourceRef:
    kind: OCIRepository
    name: manifests
